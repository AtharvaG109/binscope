# Test data manifest

This directory contains two classes of data:

- `fixtures/`: clean binaries committed for parser and report tests
- metadata files such as `samples.json`: hashes and links only

Do not commit live malware samples here.

Use the hashes and links in `samples.json` to source samples through your own controlled, legal malware-research workflow.
